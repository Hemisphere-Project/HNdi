[module]
name = hndi
description = NDI helpers: input node (NDI -> /dev/video10 -> browser <video>)
group = hndi
platforms = x86

# Delegate everything to install.sh (NDI runtime, gst plugin, v4l2loopback, service).
[install]
script = yes
